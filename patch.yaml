spec:
  template:
    spec:
      containers:
      - name: predictive-scaler
        volumeMounts:
        - name: code-volume
          mountPath: /app/app.py
          subPath: app.py
      volumes:
      - name: code-volume
        configMap:
          name: predictive-scaler-code-fix
